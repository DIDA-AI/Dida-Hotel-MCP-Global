# MCP Server configuration
HOST=0.0.0.0
PORT=8000

# DIDA API Key application page:
# https://global.rollinggo.store/apply
# (redirects to travelportal-partner-center.dida.com/register?lang=en)
#
# Auth is passed via request headers at runtime:
# Authorization: Bearer <your_api_key>
# or X-Secret-Key: <your_api_key>
